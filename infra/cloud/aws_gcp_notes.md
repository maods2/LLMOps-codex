# Cloud Readiness

- Use object storage via `S3_ENDPOINT` and `S3_BUCKET` env variables.
- Container images are compatible with AWS ECR and GCP Artifact Registry.
- Kubernetes manifests are provider-agnostic.
